# 数据读取上下文精简 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 QuickQuery/DataAnalysis 的数据读取只把「列名 + 前 5 行预览 + 统计结果」带入上下文，全量原始数据永不进入（提示词引导 + stdout 保护性截断双保险）。

**Architecture:** 三处软约束（NormalPythonExecute 工具描述、QuickQuery system prompt、DataAnalysis system prompt）引导模型「读取只打印预览、统计只 print 结果」；一处硬兜底（NormalPythonExecute.execute() 对注入 system 的 stdout 超过 50000 字符时保留头部 20000 + 尾部 30000 并加截断标记）；一处引导语微调（CompanyDataLookup.query 输出）。

**Tech Stack:** Python 3.12 / pytest / Windows 11 / PowerShell / conda 环境 `open_manus`

## Global Constraints

- Python 解释器固定为 `C:\Users\hyh\anaconda3\envs\open_manus\python.exe`，**不要用** `python` / `python3`（CLAUDE.md 强制）。
- 跑测试用 `-m pytest`（避免入口被 main.py 拦截）：`C:\Users\hyh\anaconda3\envs\open_manus\python.exe -m pytest <path> -v`。
- 提交前格式链路：`pre-commit run --all-files`（black → autoflake → isort，profile=black）。
- 代码注释用简体中文，标识符保持英文。
- `NormalPythonExecute` 的 parameters.description 经 `.format(directory=...)` 渲染——**新增文本中不得出现裸花括号 `{}`**（否则渲染报 KeyError）。
- 不修改 `max_observe`（50000）数值；不修改 `ToolCallAgent` 的 system 通道注入逻辑（Bug1 设计保留）。
- 现有测试 `tests/test_normal_python_execute.py`、`tests/test_toolcall_system_channel.py`、`tests/tool/test_company_data_lookup.py` 必须继续通过。

---

### Task 1: NormalPythonExecute 工具描述加「数据读取规范」

**Files:**
- Modify: `app/tool/chart_visualization/python_execute.py:22-41`（parameters 的 code description）
- Test: `tests/test_normal_python_execute.py`

**Interfaces:**
- Consumes: 无（纯文本改动）
- Produces: `NormalPythonExecute.parameters["properties"]["code"]["description"]` 包含「前 5 行」「严禁打印全量数据」「df.head(5)」关键词（Task 2 沿用同一文件；Task 3 的回归测试风格与本 Task 一致）

- [ ] **Step 1: 写失败测试**

在 `tests/test_normal_python_execute.py` 末尾追加（模块级函数，class 外）：

```python
def test_code_description_contains_preview_rules():
    """工具描述必须包含「数据读取规范」关键约束（回归防误删）。"""
    tool = NormalPythonExecute()
    desc = tool.parameters["properties"]["code"]["description"]
    assert "前 5 行" in desc
    assert "严禁打印全量数据" in desc
    assert "df.head(5)" in desc
    assert "df.to_string" in desc
```

- [ ] **Step 2: 运行测试确认失败**

Run: `C:\Users\hyh\anaconda3\envs\open_manus\python.exe -m pytest tests/test_normal_python_execute.py::test_code_description_contains_preview_rules -v`
Expected: FAIL（AssertionError: "前 5 行" not in desc）

- [ ] **Step 3: 修改工具描述**

`app/tool/chart_visualization/python_execute.py`，在 `code` 的 description「## 注意事项」第 3 条之后追加「## 数据读取规范」小节。原 description（第 22-37 行，注意 `{directory}` 占位符保持原样不动）：

```python
            "code": {
                "type": "string",
                "description": """要执行的 Python 代码。

## 注意事项
1. 使用 print() 输出所有结果，确保分析过程（如"数据概览"、"预处理结果"等）清晰可见
2. 将处理后的数据和报告文件保存到工作目录: {directory}
3. 报告需要内容丰富，包含完整的分析过程和对应的数据可视化
4. 可分步调用此工具，从汇总到深入进行分析

## 数据读取规范
- 读取数据文件（CSV/Excel 等）时，只打印数据预览：列名（df.columns.tolist()）、
  数据形状（df.shape）、前 5 行（df.head(5)），确认数据结构即可
- 严禁打印全量数据（df.to_string()、print(df)、遍历打印所有行等），
  全量数据不得进入对话上下文
- 统计分析脚本只 print 统计结果（聚合值、分组汇总、结论数字），
  不要 print 原始数据行，基于统计结果直接回答用户
""".format(
                    directory=config.workspace_root
                ),
            },
```

- [ ] **Step 4: 运行测试确认通过**

Run: `C:\Users\hyh\anaconda3\envs\open_manus\python.exe -m pytest tests/test_normal_python_execute.py -v`
Expected: 全部 PASS（含原有两个测试）

- [ ] **Step 5: 提交**

```bash
git add app/tool/chart_visualization/python_execute.py tests/test_normal_python_execute.py
git commit -m "feat(tool): NormalPythonExecute 工具描述加入数据读取规范（只打印预览，禁全量数据）"
```

---

### Task 2: NormalPythonExecute.execute() 保护性截断（硬兜底）

**Files:**
- Modify: `app/tool/chart_visualization/python_execute.py:43-60`（execute 方法）
- Test: `tests/test_normal_python_execute.py`

**Interfaces:**
- Consumes: Task 1 的工具描述改动（本 Task 不触碰描述文本）
- Produces: 模块级常量 `MAX_SYSTEM_CHARS = 50000`、`_HEAD_KEEP = 20000`、`_TAIL_KEEP = 30000`、模块级函数 `_truncate_stdout(observation: str) -> str`（≤阈值原样返回；>阈值返回 `head + 截断标记 + tail`）；`execute()` 成功分支的 `system` 为截断后的内容、`output` 摘要中的字符数为截断后长度。后续任务不依赖这些符号，但测试与实现需保持一致。

- [ ] **Step 1: 写失败测试**

在 `tests/test_normal_python_execute.py` 追加两个测试：

```python
@pytest.mark.asyncio
async def test_long_stdout_truncated_keeps_head_and_tail():
    """超长 stdout：system 注入应保留头部预览与尾部统计结果，加截断标记。"""
    tool = NormalPythonExecute()
    tool.sandbox = None

    # 总输出 60000 字符：HEAD_ + 25000 个 A + 30000 个 B + 5000 个 C
    code = (
        "print('HEAD_' + 'A' * 25000)\n"
        "print('B' * 30000)\n"
        "print('C' * 5000)"
    )
    result = await tool.execute(code=code)

    assert result.error is None
    assert result.system is not None
    assert "HEAD_" in result.system  # 头部保留
    assert "A" * 100 in result.system  # 头部区内容保留
    assert "C" * 100 in result.system  # 尾部区内容保留（统计结果区）
    assert "...[stdout 共" in result.system  # 截断标记存在
    assert len(result.system) < 51000  # 截断后长度受控（50000 + 标记）

    # output 摘要字符数应反映截断后的实际长度
    assert "system（" in str(result.output)
```

```python
@pytest.mark.asyncio
async def test_short_stdout_not_truncated():
    """未超阈值：保持 Bug1 原行为，全量注入 system，无截断标记。"""
    tool = NormalPythonExecute()
    tool.sandbox = None

    result = await tool.execute(code="print('A=1'); print('B=2')")

    assert result.system is not None
    assert "A=1" in result.system and "B=2" in result.system
    assert "...[stdout 共" not in result.system
    assert result.system.count("A=1") == 1
```

- [ ] **Step 2: 运行测试确认失败**

Run: `C:\Users\hyh\anaconda3\envs\open_manus\python.exe -m pytest tests/test_normal_python_execute.py -v`
Expected: 两个新测试 FAIL（`AttributeError: module '...python_execute' has no attribute 'MAX_SYSTEM_CHARS'` 或长度断言失败）；原有测试保持 PASS

- [ ] **Step 3: 实现截断逻辑**

`app/tool/chart_visualization/python_execute.py`，在 `class NormalPythonExecute` 之前新增模块级常量与截断函数：

```python
# system 通道注入上限（对齐 QuickQuery/DataAnalysis 的 max_observe=50000）
MAX_SYSTEM_CHARS = 50000
_HEAD_KEEP = 20000
_TAIL_KEEP = 30000


def _truncate_stdout(observation: str) -> str:
    """stdout 超上限时保留头部预览与尾部统计结果，中间加截断标记。"""
    if len(observation) <= MAX_SYSTEM_CHARS:
        return observation
    head = observation[:_HEAD_KEEP]
    tail = observation[-_TAIL_KEEP:]
    return (
        f"{head}\n"
        f"...[stdout 共 {len(observation)} 字符，超过 {MAX_SYSTEM_CHARS} 上限"
        f"已截断，仅保留头部预览与尾部统计结果]...\n"
        f"{tail}"
    )
```

修改 `execute()` 成功分支（原第 43-60 行整体替换为）：

```python
    async def execute(self, code: str, code_type: str | None = None, timeout=30):
        """执行 Python 代码，返回 ToolResult。

        Bug1：stdout 全量进 ToolResult.system（不截断，由 ToolCallAgent
        以 system message 形式注入下一轮 think），output 只留短摘要，
        使模型能基于完整数据回答、无需复述被截断的 observation。

        数据读取上下文精简：stdout 超过 MAX_SYSTEM_CHARS 时保护性截断，
        保留头部预览与尾部统计结果，防止模型误打印全量数据泄入上下文。
        """
        raw = await super().execute(code, timeout)
        observation = raw.get("observation", "")
        success = raw.get("success", False)

        if not success:
            return ToolResult(error=observation)

        system_content = _truncate_stdout(observation)
        return ToolResult(
            output=f"脚本执行成功，stdout 已载入 system（{len(system_content)} 字符）",
            system=system_content,
        )
```

- [ ] **Step 4: 运行测试确认通过**

Run: `C:\Users\hyh\anaconda3\envs\open_manus\python.exe -m pytest tests/test_normal_python_execute.py -v`
Expected: 全部 PASS（原 2 个 + 新增 3 个，共 5 个）

- [ ] **Step 5: 提交**

```bash
git add app/tool/chart_visualization/python_execute.py tests/test_normal_python_execute.py
git commit -m "feat(tool): NormalPythonExecute stdout 注入加 50000 字符保护性截断（头 2 万 + 尾 3 万）"
```

---

### Task 3: QuickQuery / DataAnalysis system prompt 加「数据读取与输出规范」

**Files:**
- Modify: `app/prompt/quick_query.py:40-49`（SYSTEM_PROMPT 的「## 约束」）
- Modify: `app/prompt/visualization.py:7-18`（SYSTEM_PROMPT 的「# 数据与环境」之后）
- Create: `tests/test_prompt_preview_rules.py`

**Interfaces:**
- Consumes: 无
- Produces: `app.prompt.quick_query.SYSTEM_PROMPT` 与 `app.prompt.visualization.SYSTEM_PROMPT` 包含「前 5 行」「严禁打印全量数据」关键词；新建回归测试文件 `tests/test_prompt_preview_rules.py`

- [ ] **Step 1: 写失败测试**

新建 `tests/test_prompt_preview_rules.py`：

```python
"""数据读取上下文精简：Agent system prompt 数据读取规范回归测试。

防止未来编辑提示词时误删「只打印预览、禁全量数据」约束。
"""
from app.prompt.quick_query import SYSTEM_PROMPT as QQ_PROMPT
from app.prompt.visualization import SYSTEM_PROMPT as VIZ_PROMPT


def test_quick_query_prompt_contains_preview_rules():
    assert "前 5 行" in QQ_PROMPT
    assert "严禁打印全量数据" in QQ_PROMPT
    assert "df.to_string" in QQ_PROMPT


def test_visualization_prompt_contains_preview_rules():
    assert "前 5 行" in VIZ_PROMPT
    assert "严禁打印全量数据" in VIZ_PROMPT
    assert "df.to_string" in VIZ_PROMPT
```

- [ ] **Step 2: 运行测试确认失败**

Run: `C:\Users\hyh\anaconda3\envs\open_manus\python.exe -m pytest tests/test_prompt_preview_rules.py -v`
Expected: 两个测试 FAIL（AssertionError）

- [ ] **Step 3: 修改 QuickQuery prompt**

`app/prompt/quick_query.py`，在「## 约束」中「- **不要复述 python_execute 的原始输出**」之前插入：

```
- **数据读取只输出预览**：python_execute 读取 CSV 时只打印列名、数据形状和前 5 行，
  严禁打印全量数据（df.to_string()/print(df)），全量数据不得进入对话上下文
- **只把统计结果带进上下文**：统计脚本只 print 聚合值、分组汇总、结论数字，
  基于这些统计结果直接回答用户
```

- [ ] **Step 4: 修改 DataAnalysis prompt**

`app/prompt/visualization.py`，在「# 数据与环境」第 4 条（"4. 最终生成分析结论报告"）之后新增小节：

```
# 数据读取与输出规范
- python_execute 读取数据文件时只打印数据预览（列名、数据形状、前 5 行）
- 严禁打印全量数据（df.to_string()/print(df)），全量数据不得进入对话上下文
- 统计脚本只 print 统计结果与关键结论，图表与报告基于统计结果生成
```

- [ ] **Step 5: 运行测试确认通过**

Run: `C:\Users\hyh\anaconda3\envs\open_manus\python.exe -m pytest tests/test_prompt_preview_rules.py -v`
Expected: 2 个测试 PASS

- [ ] **Step 6: 提交**

```bash
git add app/prompt/quick_query.py app/prompt/visualization.py tests/test_prompt_preview_rules.py
git commit -m "feat(prompt): QuickQuery/DataAnalysis system prompt 加入数据读取与输出规范"
```

---

### Task 4: CompanyDataLookup.query 输出追加预览引导语

**Files:**
- Modify: `app/tool/company_data_lookup.py:527`（`_execute_query` 的 output 提示语）
- Test: `tests/tool/test_company_data_lookup.py`（`TestExecuteQueryDataIntegrity` 类内新增方法）

**Interfaces:**
- Consumes: `TestExecuteQueryDataIntegrity._FakeConn` / `_FakeCursor`（本文件内已有，直接复用）
- Produces: `_execute_query` 成功时 output 末尾多一句「读取该 CSV 时只打印预览（列名与前 5 行），请勿打印全量数据。」

- [ ] **Step 1: 写失败测试**

在 `tests/tool/test_company_data_lookup.py` 的 `TestExecuteQueryDataIntegrity` 类内新增方法：

```python
    def test_query_output_advises_preview_only(self, tmp_path, monkeypatch):
        """query 输出应引导后续 read_csv 只打印预览，不打印全量数据。"""
        tool = CompanyDataLookup()
        tool.sandbox = None
        tool.workspace_dir = str(tmp_path)
        tool.host_workspace_dir = str(tmp_path)
        fake = self._FakeConn(description=[("total",)], rows=[(6566,)])
        monkeypatch.setattr(tool, "_get_connection", lambda **kw: fake)

        result = tool._execute_query("SELECT COUNT(*) AS total FROM fa_card_orders")

        assert result.error is None
        assert result.output is not None
        assert "只打印预览" in result.output
        assert "前 5 行" in result.output
        assert "请勿打印全量数据" in result.output
```

- [ ] **Step 2: 运行测试确认失败**

Run: `C:\Users\hyh\anaconda3\envs\open_manus\python.exe -m pytest tests/tool/test_company_data_lookup.py::TestExecuteQueryDataIntegrity::test_query_output_advises_preview_only -v`
Expected: FAIL（AssertionError: "只打印预览" not in output）

- [ ] **Step 3: 修改输出提示语**

`app/tool/company_data_lookup.py` 第 527 行，原：

```python
                f"请使用 python_execute (pandas.read_csv('{tell_path}')) 读取该文件继续分析。"
```

改为：

```python
                f"请使用 python_execute (pandas.read_csv('{tell_path}')) 读取该文件继续分析。"
                f"读取该 CSV 时只打印预览（列名与前 5 行），请勿打印全量数据。"
```

（注意：这是在 f-string 参数列表内追加一个相邻字符串字面量，原行与新增行并列即可，无需改其他代码。）

- [ ] **Step 4: 运行测试确认通过**

Run: `C:\Users\hyh\anaconda3\envs\open_manus\python.exe -m pytest tests/tool/test_company_data_lookup.py -v`
Expected: 全部 PASS（原有 + 新增 1 个）

- [ ] **Step 5: 提交**

```bash
git add app/tool/company_data_lookup.py tests/tool/test_company_data_lookup.py
git commit -m "feat(tool): CompanyDataLookup.query 输出追加「只打印预览」引导语"
```

---

### Task 5: 全量回归验证

**Files:**
- Test: 全量 `tests/`

**Interfaces:**
- Consumes: Task 1-4 全部改动

- [ ] **Step 1: 跑全量测试**

Run: `C:\Users\hyh\anaconda3\envs\open_manus\python.exe -m pytest tests/ -v`
Expected: 全部 PASS。若 `tests/web/` 因 MySQL 配置/网络不可用失败，记录并单独说明（按 AGENTS.md：web 测试依赖 `[web]` MySQL 配置）；`tests/sandbox/` 需 Docker Desktop，若未运行也需单独说明。**必须保证与本次改动相关的测试（tests/test_normal_python_execute.py、tests/test_prompt_preview_rules.py、tests/tool/test_company_data_lookup.py、tests/test_toolcall_system_channel.py、tests/test_quick_query_set_sandbox.py、tests/test_data_analysis_set_sandbox.py）全部通过。**

- [ ] **Step 2: 格式检查**

Run: `pre-commit run --all-files`
Expected: 无错误（或仅修正本计划涉及的 4 个 Python 文件后通过）

- [ ] **Step 3: 提交（若格式修正产生变更）**

```bash
git add -u
git commit -m "chore: 格式化"
```

- [ ] **Step 4: 验收汇总**

对照设计文档 `docs/superpowers/specs/2026-08-13-数据读取上下文精简-design.md` 逐项核对：
- [ ] 改动点 1（工具描述规范）→ Task 1
- [ ] 改动点 2（50000 字符截断，头 2 万 + 尾 3 万）→ Task 2
- [ ] 改动点 3（QuickQuery prompt）→ Task 3
- [ ] 改动点 4（DataAnalysis prompt）→ Task 3
- [ ] 改动点 5（query 引导语）→ Task 4
- [ ] 测试计划（截断单测 + 提示词回归 + 现有测试回归）→ Task 1-5

## 备注

- Windows 下 `multiprocessing` 本地执行路径已被现有测试验证可用（`tests/test_normal_python_execute.py` 原有测试即走该路径），Task 2 的超长输出测试沿用同一路径，无需 Docker。
- 若 Task 2 测试因 Windows 本地执行超时（60000 字符 print 极快，正常不会），可改用 monkeypatch 替换 `_execute_local` 直接返回构造的 dict，断言逻辑不变。
