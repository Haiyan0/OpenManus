# OpenManus 启动项说明

本项目提供了 **4 个不同入口脚本**，分别对应不同的使用场景和运行模式。下面逐一说明每个启动项的用途、架构差异和适用场景。

---

## 总览对比

| 启动项 | 核心智能体 | 架构模式 | 沙箱隔离 | 支持 MCP | 适用场景 |
|---|---|---|---|---|---|
| `main.py` | `Manus` | 单智能体 ReAct | ❌ 本地执行 | ✅ 动态连接 | 日常通用任务 |
| `run_flow.py` | `Manus` + `DataAnalysis`（可选） | 多智能体编排 | ❌ 本地执行 | ❌ | 复杂多步骤任务 |
| `run_mcp.py` | `MCPAgent` | 单智能体 ReAct | ❌ 本地执行 | ✅ 外部 MCP 服务器 | 对接外部工具服务 |
| `run_mcp_server.py` | 无（作为服务端） | MCP 服务端暴露工具 | ❌ 本地执行 | — | 将 OpenManus 工具暴露给其他智能体 |

---

## 1. `main.py` — 通用单智能体（⭐ 最常用）

### 启动命令

```bash
python main.py                      # 交互模式
python main.py --prompt "你的任务"   # 单次执行
```

### 架构简图

```
用户输入 → Manus (ReAct) → 本地工具集
                │
                ├── PythonExecute（子进程执行 Python）
                ├── BrowserUseTool（Playwright 浏览器自动化）
                ├── StrReplaceEditor（文件查看/编辑）
                ├── AskHuman（向你询问）
                ├── Terminate（结束任务）
                └── MCP 工具（动态连接远程工具）
```

### 使用什么智能体

`Manus` — 项目的**主力通用智能体**。继承自 `ToolCallAgent → ReActAgent → BaseAgent`。

### 工作流程

1. 用户输入任务
2. `Manus.think()` 将对话上下文 + 工具列表发给 LLM
3. LLM 返回文本回复或工具调用
4. `Manus.act()` 执行工具调用，结果写入记忆
5. 循环直到 LLM 主动调用 `terminate` 或达到 20 步上限
6. 当 LLM 返回纯文本（无工具调用）时自动结束

### 何时使用

- ✅ **日常通用任务**：写代码、查信息、文件操作、网页浏览
- ✅ **快速原型**：一条指令完成简单到中等复杂度的任务
- ✅ **单个 LLM 驱动**：不需要多个智能体协作的场景

### 何时不用

- ❌ 需要多智能体分工协作的复杂任务 → 用 `run_flow.py`

---

## 2. `run_flow.py` — 多智能体编排（规划流）

### 启动命令

```bash
python run_flow.py
# 交互模式下输入任务描述
```

### 架构简图

```
用户输入 → PlanningFlow（编排层）
                │
                ├── LLM 生成执行计划（步骤 1, 2, 3, …）
                │
                ├── Manus（通用执行智能体）
                │      └── 执行大部分步骤
                │
                └── DataAnalysis（可选，需在 config.toml 中启用）
                       └── 专门处理数据分析/可视化步骤
```

### 使用什么智能体

- `PlanningFlow` — 管理计划创建、步骤分派和状态跟踪
- `Manus` — 默认执行器，处理大部分步骤
- `DataAnalysis` — 可选的专精智能体，处理数据分析与可视化任务（需安装额外依赖）

### 工作流程

1. LLM 分析用户任务，自动拆解为多步骤计划
2. `PlanningFlow` 将每个步骤分派给合适的执行智能体
3. 支持步骤依赖（前一步的输出作为后一步的输入）
4. 整个流程最长运行 1 小时（3600 秒超时）

### 配置启用 DataAnalysis

```toml
# config/config.toml
[runflow]
use_data_analysis_agent = true  # 改为 true 激活
```

还需安装可视化依赖：参考 `app/tool/chart_visualization/README_zh.md`

### 何时使用

- ✅ **复杂多步骤任务**：例如"研究某主题 → 写报告 → 可视化数据 → 生成摘要"
- ✅ **需要计划拆解**：任务步骤有依赖关系，需要先规划再执行
- ✅ **多专精协作**：通用任务 + 数据可视化等专业场景组合

### 何时不用

- ❌ 简单单步任务 → 用 `main.py` 更快
- ❌ 纯对话/查资料 → `main.py` 足够

---

## 3. `run_mcp.py` — MCP 客户端模式

### 启动命令

```bash
# 标准输入输出模式（默认）
python run_mcp.py

# SSE 网络连接模式
python run_mcp.py -c sse --server-url http://127.0.0.1:8000/sse

# 交互模式（持续对话）
python run_mcp.py -i

# 单次执行
python run_mcp.py -p "你的任务"
```

### 参数说明

| 参数 | 说明 |
|---|---|
| `-c, --connection` | 连接类型：`stdio` 或 `sse` |
| `--server-url` | SSE 连接时的服务器地址 |
| `-i, --interactive` | 交互模式，持续对话直到输入 `exit` |
| `-p, --prompt` | 单次任务执行 |

### 架构简图

```
                 MCP 外部服务器
                 (Bash / Browser / Editor / …)
                      │
              stdio 或 SSE 协议
                      │
用户输入 → MCPAgent (ReAct) → 使用远程 MCP 工具执行任务
```

### 使用什么智能体

`MCPAgent` — 一个精简的 `ToolCallAgent`，**没有本地工具**，完全依赖外部 MCP 服务器提供的工具。

### 工作流程

1. 启动时连接到指定的 MCP 服务器（stdio 或 SSE）
2. 从 MCP 服务器获取可用工具列表
3. LLM 根据用户任务调用远程工具
4. 支持交互模式（`-i`）持续对话

### 何时使用

- ✅ **已有外部 MCP 工具服务**：你想用别人的工具来完成任务
- ✅ **工具与智能体分离部署**：工具运行在另一台机器/进程上
- ✅ **测试 MCP 连接**：验证 MCP 服务器是否正常工作

### 何时不用

- ❌ 不需要外部 MCP 工具 → `main.py` 已内置丰富的本地工具
- ❌ 你想暴露自己的工具给别人用 → 那应该用 `run_mcp_server.py`

---

## 4. `run_mcp_server.py` — MCP 服务端模式

### 启动命令

```bash
python run_mcp_server.py
```

### 架构简图

```
OpenManus 工具（Bash / Browser / Editor / Terminate）
            │
            │  FastMCP 封装
            │
       MCP 服务端（stdio 或 SSE）
            │
            │  MCP 协议
            │
      外部智能体（Claude Desktop / 其他 AI 应用）
```

### 核心理念

**将 OpenManus 的工具能力暴露为 MCP 服务**，让其他 AI 应用（如 Claude Desktop）能够调用 OpenManus 的工具。

### 暴露了哪些工具

| 工具名 | 功能 |
|---|---|
| `bash` | Shell 命令执行 |
| `browser` | Playwright 浏览器自动化 |
| `editor` | 文件查看/编辑（字符串替换） |
| `terminate` | 结束会话 |

### 何时使用

- ✅ **给 Claude Desktop 等外部 AI 应用提供工具**：让 Claude 拥有浏览器、Shell、文件编辑能力
- ✅ **工具能力复用**：一套 OpenManus 工具，多个智能体共享
- ✅ **构建工具生态**：OpenManus 作为"工具提供者"角色

### 何时不用

- ❌ 你要自己用智能体完成任务 → 用 `main.py`
- ❌ 你要连接别人的 MCP 工具 → 用 `run_mcp.py`

---

## 场景速查表

| 我想做什么 | 用哪个 |
|---|---|
| 让 AI 帮我写代码、查资料、浏览网页 | `python main.py` |
| 复杂任务需要先规划再分步执行 | `python run_flow.py` |
| 任务涉及数据分析和图表生成 | 在 `run_flow.py` 中启用 `DataAnalysis` |
| 连接外部 MCP 工具服务 | `python run_mcp.py -c sse --server-url …` |
| 让 Claude Desktop 用 OpenManus 的工具 | `python run_mcp_server.py` |
| 就试试能不能跑起来 | `python main.py --prompt "你好"` |
